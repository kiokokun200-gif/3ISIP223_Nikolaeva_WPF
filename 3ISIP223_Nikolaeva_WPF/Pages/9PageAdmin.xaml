<Page x:Class="_3ISIP223_Nikolaeva_WPF.Pages._9PageAdmin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_3ISIP223_Nikolaeva_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="_9PageAdmin">



    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Управление пользователями" FontSize="30" FontWeight="Bold" Margin="10"/>

        <!-- Список пользователей -->
        <ListBox Grid.Row="1" x:Name="ListBoxUsers" Margin="10" d:ItemsSource="{d:SampleData ItemCount=5}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightPink" BorderThickness="1" CornerRadius="5" Margin="5" Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding LastName}" FontSize="20" FontWeight="Bold"/>
                                    <TextBlock Text=" " FontSize="20"/>
                                    <TextBlock Text="{Binding FirstName}" FontSize="20" FontWeight="Bold"/>
                                    <TextBlock Text=" " FontSize="20"/>
                                    <TextBlock Text="{Binding MiddleName}" FontSize="20" FontWeight="Bold"/>
                                </StackPanel>
                                <TextBlock Text="{Binding PhoneNumber, StringFormat=Телефон: {0}}" FontSize="16" Foreground="Gray"/>
                                <TextBlock Text="{Binding Role.Name, StringFormat=Роль: {0}}" FontSize="16"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="20">
                                <Button Content="Изменить роль" Width="200" Height="35" 
                                    Click="BtnChangeRole_Click" Tag="{Binding}"/>
                                <Button Content="Удалить" Width="100" Height="35" Margin="10,0,0,0"
                                    Click="BtnDeleteUser_Click" Tag="{Binding}"
                                    Background="#FF4444"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Кнопка назад -->
        <Button Grid.Row="2" Content="← Назад" Width="100" Height="35" Margin="10" 
            HorizontalAlignment="Left" Click="BtnBack_Click"/>
    </Grid>
</Page>