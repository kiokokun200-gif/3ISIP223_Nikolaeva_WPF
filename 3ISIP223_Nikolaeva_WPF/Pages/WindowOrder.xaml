<Window x:Class="_3ISIP223_Nikolaeva_WPF.Pages.WindowOrder"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_3ISIP223_Nikolaeva_WPF.Pages"
        mc:Ignorable="d"
        Title="WindowOrder" Height="450" Width="800">
    <Grid>
        <StackPanel >
            <TextBlock Text="Подтверждение заказа" FontSize="30" FontWeight="Bold" Margin="0 0 0 10"/>
            <TextBlock Text="Выберите способ оплаты:" FontSize="22" />
            <ComboBox x:Name="ComboBoxPayments" Width="150" FontSize="22" HorizontalAlignment="Left" Height="40" Margin="0 10 0 10"/>
            <TextBlock Text="Выберите дату получения" FontSize="22" Margin="0 10 0 0"/>
            <ListBox x:Name="ListBoxDates">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Button x:Name="BtnDate" Content="{Binding}" ContentStringFormat="dd.MM.yyyy" FontSize="18"  Width="130" Height="30" Click="BtnDate_Click"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>

            <TextBlock Text="Итоговая цена: " FontSize="22"/>
            <TextBlock x:Name="TxtBlockTotalPrice" FontSize="22"/>

            <!--<Button x:Name="BtnConfirm" Content="Подтвердить заказ" FontSize="22" Width="200" Height="40" HorizontalAlignment="Left" Margin="0 10 0 0" Click="BtnConfirm_Click"/>-->
        </StackPanel>
    </Grid>
</Window>
