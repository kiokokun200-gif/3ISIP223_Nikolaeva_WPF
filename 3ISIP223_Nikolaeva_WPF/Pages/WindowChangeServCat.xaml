<Window x:Class="_3ISIP223_Nikolaeva_WPF.Pages.WindowChangeServCat"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_3ISIP223_Nikolaeva_WPF.Pages"
        mc:Ignorable="d"
        Title="WindowChangeServCat" Height="200" Width="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Тип услуги: " Margin="0 3 0 0"/>
            <TextBox x:Name="TxtBoxServCarName" Height="40" Width="200" Text="{Binding Name}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Описание типа услуги: " />
            <TextBox x:Name="TxtBoxServCarDesc" Height="70" Width="300" Margin="0 0 10 0" Text="{Binding Description}"/>
        </StackPanel>
        <Button Content="Сохранить" x:Name="BtnSave" Grid.Row="2" Width="300" Margin="0 10 0 0" Click="BtnSave_Click"/>

    </Grid>
</Window>
