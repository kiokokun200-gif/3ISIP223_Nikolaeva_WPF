<Page x:Class="_3ISIP223_Nikolaeva_WPF.Pages._1MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_3ISIP223_Nikolaeva_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="_1MainPage">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Text="Конфигуратор ПК" FontSize="30" HorizontalAlignment="Left"/>
        <ListBox x:Name="PartTypesListBox" Grid.Column="0" Width="400" ScrollViewer.VerticalScrollBarVisibility="Disabled" Grid.ColumnSpan="2" d:ItemsSource="{d:SampleData ItemCount=5}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Width="400" HorizontalAlignment="Right">
                        <!--<Image x:Name="ImageBox" Source="{Binding picture}" Width="20" Height="20" Margin="10"/>-->
                        <TextBlock Text="{Binding name}" FontSize="20" Margin="10"/>
                        <Button x:Name="BtnChoose" Content="Выбрать" Margin="10" Click="BtnChoose_Click"/>
                    </StackPanel>
                </DataTemplate>

            </ListBox.ItemTemplate>
            <!--<ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>-->
        </ListBox>
    </Grid>
</Page>
