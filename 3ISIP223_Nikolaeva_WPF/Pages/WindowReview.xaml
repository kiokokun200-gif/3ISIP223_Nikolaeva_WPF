<Window x:Class="_3ISIP223_Nikolaeva_WPF.Pages.WindowReview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_3ISIP223_Nikolaeva_WPF.Pages"
        mc:Ignorable="d"
        Title="WindowReview" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Оставить отзыв"/>
            <ListBox x:Name="ListBoxReviewCateg" d:ItemsSource="{d:SampleData ItemCount=5}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}"/>
                            <ComboBox x:Name="ComboBoxRating" SelectedIndex="0" ItemsSource="{Binding ratings}" Width="250"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock Text="Комментарий: "/>
            <TextBox x:Name="TxtBoxReviewComment" Height="100" Margin="10"/>
            <Button x:Name="BtnAddReview" Content="Добавить отзыв" Width="200" Margin="10" Click="BtnAddReview_Click"/>
        </StackPanel>
        
        
    </Grid>
</Window>
