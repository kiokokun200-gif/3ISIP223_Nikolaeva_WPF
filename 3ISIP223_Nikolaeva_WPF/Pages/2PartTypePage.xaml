<Page x:Class="_3ISIP223_Nikolaeva_WPF.Pages._2PartTypePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_3ISIP223_Nikolaeva_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="_2PartTypePage">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="⌕" FontSize="30"/>
            <TextBox x:Name="TxtBoxSearch" Width="200" Height="30" TextChanged="TxtBoxSearch_TextChanged"/>
        
        </StackPanel>

        <ListBox x:Name="PartsListBox" Grid.Row="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled" d:ItemsSource="{d:SampleData ItemCount=5}" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <!--<WrapPanel Orientation="Horizontal">
                        <Image Source="{Binding image}" Width="150" Height="150" Margin="10"/>
                        <WrapPanel>
                        <TextBlock Text="{Binding name}" Margin="10" FontSize="22" />
                        <TextBlock Text="{Binding FullDescription, StringFormat=[{0}]}" Margin="10" FontSize="22" TextWrapping="Wrap"/>
                        </WrapPanel>
                        <TextBlock Text="{Binding price}"  FontSize="30"/>
                    </WrapPanel>-->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>

                        
                        <Image Grid.Column="0" Source="{Binding image}" Width="150" Height="150" Margin="10"/>
                        <WrapPanel Grid.Column="1">
                            <TextBlock Text="{Binding name}" Margin="0 10 5 0" FontSize="22" />
                            <TextBlock Text="{Binding FullDescription, StringFormat=[{0}]}" Margin="10" FontSize="22" TextWrapping="WrapWithOverflow"/>
                        </WrapPanel>
                        <StackPanel Grid.Column="2" Orientation="Vertical">
                            <TextBlock Text="{Binding price, StringFormat='{}{0} $'}" FontSize="30"/>
                            <!--<TextBlock Text=" $" FontSize="15" Margin="0 15 0 0 "/>-->
                            <Button x:Name="BtnAddPart" Content="Добавить" Background="#FFFF3394" Height="30" FontSize="15" BorderThickness="0" Foreground="White" Click="BtnAddPart_Click"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
        
            </ListBox.ItemTemplate>

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        
    </Grid>
</Page>
