<Page x:Class="_3ISIP223_Nikolaeva_WPF.Pages._6PageAuthor"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_3ISIP223_Nikolaeva_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="_6PageAuthor">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel HorizontalAlignment="Left" Grid.Row="0" Margin="10">
            <TextBlock Text="{Binding NickName}" FontSize="30" FontWeight="Bold"/>
            <TextBlock Text="{Binding Email}" />
            <TextBlock Text="{Binding Login}"/>
            <TextBlock Text="{Binding Role.Name}"/>
        </StackPanel>
        <Button x:Name="BtnAddNewBook" Content="Добавить новую книгу" Style="{StaticResource BtnsMenu}" Grid.Row="1" Click="BtnAddNewBook_Click"/>

        <TabControl Grid.Row="2">
            <TabItem Header="Мои книги">
                <ListBox x:Name="ListBoxBooks"  d:ItemsSource="{d:SampleData ItemCount=5}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="{Binding CoverImage}" Width="100" Height="120"/>
                                    <StackPanel Margin="0 0 0 10" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>

                                    </StackPanel>
                                </StackPanel>

                                <Button x:Name="BtnEditBook" Content="Редактировать" Grid.Column="1" Style="{StaticResource BtnsMenu}" Height="40" HorizontalAlignment="Right" Margin="10 0 10 0" Click="BtnEditBook_Click"/>

                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>

            <TabItem Header="Мои замороженные книги">
                <ListBox x:Name="ListBoxFrozenBooks"  d:ItemsSource="{d:SampleData ItemCount=5}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="{Binding CoverImage}" Width="100" Height="120"/>
                                    <StackPanel Margin="0 0 0 10" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold"/>

                                    </StackPanel>
                                </StackPanel>

                                <Button x:Name="BtnDefrozeBook" Content="Оспорить заморозку" Grid.Column="1" Style="{StaticResource BtnsMenu}" Height="40" HorizontalAlignment="Right" Margin="10 0 10 0" Click="BtnDefrozeBook_Click"/>

                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
        </TabControl>
        
    </Grid>
</Page>
