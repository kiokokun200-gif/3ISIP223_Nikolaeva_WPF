<Window x:Class="_3ISIP223_Nikolaeva_WPF.Pages.WindowChangeManufacturer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_3ISIP223_Nikolaeva_WPF.Pages"
        mc:Ignorable="d"
        Title="WindowChangeManufacturer" Height="150" Width="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Изменение производителя"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0 10 0 0">
            <TextBlock Text="Название: "/>
            <TextBox Text="{Binding Name}" x:Name="TxtBoxChangeName" Width="200"/>
        </StackPanel>

        <Button Content="Сохранить" x:Name="BtnSave" Grid.Row="2" Width="300" Margin="0 10 0 0" Click="BtnSave_Click"/>
    </Grid>
</Window>
