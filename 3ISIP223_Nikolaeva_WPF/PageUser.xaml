<Page x:Class="_3ISIP223_Nikolaeva_WPF.PageUser"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_3ISIP223_Nikolaeva_WPF"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageUser">

    <Grid Background="White">
        <StackPanel>
            <TextBlock Text="{Binding Username}"/>
            <TextBlock Text="{Binding Email}"/>
            <TextBlock Text="{Binding Organization}"/>

            <TextBlock Text="Конференции"/>
            <ListBox x:Name="ListBoxConferences" d:ItemsSource="{d:SampleData ItemCount=5}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}"/>
                            <TextBlock Text="{Binding ConferenceDate, StringFormat={}{0:yyyy.MM.dd}}"/>
                            <TextBlock Text="{Binding Location}"/>

                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </StackPanel>
        
    </Grid>
</Page>
